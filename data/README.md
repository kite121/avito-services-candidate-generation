# Local data

Put the supplied files here:

- `train.parquet`
- `benchmark_queries.parquet`
- `benchmark_items.parquet`

This directory is ignored by Git. Do not commit source data, downloaded model
weights or generated submissions to the repository.
